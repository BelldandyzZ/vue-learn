# 一、Vue简介

```
网络通信：axios

页面跳转：Vue-router

状态管理：vuex


VueUI：主要是html层面的，如ElementUI


Vue主要是通过ViewModel把数据与前端页面绑定，类似于MVC中的Model用于存放数据。后端可以通过ViewModel拿到数据


Vue是MVVM模式，MVVM是基于MVC模式发展来的，分为视图层展示数据，模型存储数据，控制器操作数据。View的值是从Model中取的，而Model
的数据是从后端传过来的，View和Model的数据为双向绑定
```



# 二、Vue指令

